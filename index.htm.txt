<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Это для тебя</title>
  <style>
    :root{
      --bg:#050507;
      --fg:#f1f1f6;
      --muted:#a7a7bb;
      --gold:#d6b35a;
      --card:rgba(255,255,255,.04);
      --border:rgba(255,255,255,.08);
      --shadow: 0 20px 70px rgba(0,0,0,.60);
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      min-height:100svh;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:26px;
      color:var(--fg);
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      background:
        radial-gradient(1200px 700px at 50% -10%, rgba(214,179,90,.10), transparent 60%),
        radial-gradient(900px 500px at 80% 10%, rgba(255,255,255,.05), transparent 55%),
        var(--bg);
    }
    .wrap{width:min(860px, 100%);}
    .card{
      background:var(--card);
      border:1px solid var(--border);
      border-radius:26px;
      padding:30px 28px;
      box-shadow:var(--shadow);
      backdrop-filter: blur(10px);
    }
    .top{
      display:flex;
      gap:16px;
      align-items:center;
      margin-bottom:14px;
    }
    .heart{
      width:46px;height:46px;
      flex:0 0 46px;
      display:grid;
      place-items:center;
      border-radius:16px;
      border:1px solid rgba(214,179,90,.35);
      background: rgba(214,179,90,.08);
      position:relative;
      overflow:hidden;
    }
    .pulse{
      position:absolute;
      inset:-40%;
      border-radius:999px;
      border:1px solid rgba(214,179,90,.35);
      animation:pulse 1.6s infinite;
      opacity:.35;
    }
    @keyframes pulse{
      0%{transform:scale(.55); opacity:0}
      20%{opacity:.35}
      70%{opacity:.10}
      100%{transform:scale(1.15); opacity:0}
    }
    .heart svg{
      filter: drop-shadow(0 6px 20px rgba(214,179,90,.22));
    }
    h1{
      margin:0;
      font-size:26px;
      letter-spacing:.02em;
      font-weight:750;
    }
    .sub{
      margin:6px 0 0;
      color:var(--muted);
      font-size:14.5px;
      line-height:1.55;
    }
    .divider{
      height:1px;
      background:rgba(255,255,255,.08);
      margin:18px 0 18px;
    }
    .text{
      font-size:16px;
      line-height:1.85;
      letter-spacing:.01em;
    }
    .text p{margin:0 0 14px}
    .mono{
      margin:14px 0 18px;
      padding:14px 14px;
      border-radius:16px;
      border:1px solid var(--border);
      background: rgba(0,0,0,.34);
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
      font-size:14px;
      line-height:1.7;
      color:#f3f3f7;
      white-space:pre-wrap;
    }
    .videoTitle{
      display:inline-block;
      padding:8px 10px;
      border-radius:14px;
      border:1px solid rgba(214,179,90,.35);
      background: rgba(214,179,90,.12);
      color:var(--fg);
      font-weight:750;
    }
    ul{
      margin:10px 0 14px 0;
      padding:0;
      list-style:none;
    }
    li{
      margin:8px 0;
      display:flex;
      gap:10px;
      align-items:flex-start;
    }
    .dash{
      color:rgba(214,179,90,.95);
      line-height:1.8;
      font-weight:700;
    }
    .foot{
      margin-top:18px;
      color:var(--muted);
      font-size:14px;
      line-height:1.65;
    }
    .hook{
      margin-top:16px;
      padding:14px 14px;
      border-radius:18px;
      border:1px solid rgba(214,179,90,.22);
      background: rgba(214,179,90,.06);
      color:var(--fg);
      font-weight:650;
    }
    @media (max-width:520px){
      h1{font-size:22px}
      .text{font-size:15.5px}
      .card{padding:22px 18px}
    }
  </style>
</head>
<body>
  <main class="wrap">
    <section class="card" role="article" aria-label="Инструкция">
      <div class="top">
        <div class="heart" aria-hidden="true">
          <div class="pulse"></div>
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
            <path d="M12 21s-7-4.35-9.5-8.6C.3 8.8 2.2 5.8 5.4 5.1c1.9-.4 3.6.4 4.6 1.7 1-1.3 2.7-2.1 4.6-1.7 3.2.7 5.1 3.7 2.9 7.3C19 16.65 12 21 12 21z"
                  stroke="rgba(214,179,90,.95)" stroke-width="1.6" />
          </svg>
        </div>
        <div>
          <h1>Это для тебя. И только для тебя.</h1>
          <div class="sub">Если ты это читаешь — значит всё идёт по плану</div>
        </div>
      </div>

      <div class="divider"></div>

      <div class="text">
        <p>
          Не отвечай никому<br/>
          Не спрашивай, кто отправил цветы<br/>
          Не пытайся угадать
        </p>

        <p>Сейчас это не важно</p>
        <p>Важно только то, что будет дальше</p>

        <div class="mono">Прежде чем продолжить:</div>

        <ul>
          <li><span class="dash">—</span><span>Убедись, что ты одна</span></li>
          <li><span class="dash">—</span><span>В комнате тихо</span></li>
          <li><span class="dash">—</span><span>Никто не увидит экран</span></li>
          <li><span class="dash">—</span><span>Ты готова уделить этому несколько минут</span></li>
        </ul>

        <p class="foot">
          Если хотя бы один пункт не выполнен — закрой страницу<br/>
          Она подождёт
        </p>

        <div class="divider"></div>

        <p>Когда будешь готова:</p>

        <p>
          Включи телевизор<br/>
          Открой приложение VK
        </p>

        <p>В поиске введи название:</p>
        <p><span class="videoTitle">Даже если закончатся слова</span></p>

        <p>
          Найди видео<br/>
          Не открывай сразу
        </p>

        <p>Вернись сюда</p>

        <p>
          Когда будешь стоять перед экраном<br/>
          и будешь готова
        </p>

        <p>просто запусти видео</p>

        <div class="hook">
          Ты уже сделала первый шаг<br/>
          Остался ещё один
        </div>
      </div>
    </section>
  </main>
</body>
</html>
